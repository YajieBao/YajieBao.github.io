# Yajie Bao — Academic Homepage

The root `index.html` and `research.html` files are standalone pages and can be
opened directly in a browser. The repository also keeps its Jekyll configuration
for GitHub Pages compatibility.

For a Jekyll preview:

```bash
bundle install
bundle exec jekyll serve
```

Then open `http://localhost:4000`.
